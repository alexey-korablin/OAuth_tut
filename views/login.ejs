<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Login</title>
    <style>
      body {
        font-family: arial, sans-serif;
        margin: 0;
      }

      nav {
        background-color: #ff5353;
        padding: 20px 10px;
      }

      nav ul {
        max-width: 960px;
        margin: 0 auto;
        padding: 0;
      }

      nav li {
        display: inline-block;
        margin: 0 10px 0 0;
      }

      nav a {
        color: #ffffff;
        font-size: 18px;
        background-color: rgba(255, 255, 255, 0.2);
        padding: 10px;
        display: block;
      }

      main,
      header {
        min-width: 960px;
        margin: 30px auto;
        padding: 0 10;
        color: #333333;
      }

      .google-btn {
        color: #ffffff;
        font-size: 18px;
        padding: 10px;
        background-color: #ff5353;
      }
    </style>
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/auth/logout">Logout</a></li>
        <li><a href="/auth/login/">Login</a></li>
        <li><a href="/">Home page</a></li>
      </ul>
    </nav>
    <header>
      <h1>Login using...</h1>
    </header>
    <main><a href="/auth/google" class="google-btn">Google+</a></main>
  </body>
</html>
